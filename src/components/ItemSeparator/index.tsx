import { View, StyleSheet } from "react-native";

export const ItemSeparator = () => {
  return (
    <View
      style={{
        height: StyleSheet.hairlineWidth,
        backgroundColor: "#eee",
      }}
    />
  );
};
